import styles from './Footer.module.css';

function Footer(){
    return(
        <footer className={styles.footer}>
            <h2>Desenvolvido por Lucas Martins &reg; 2024</h2>
        </footer>
    );
}

export default Footer;